---
const base = import.meta.env.BASE_URL;
const links = [
  { href: `${base}journal/`, label: 'Journal' },
  { href: `${base}articles/`, label: 'Articles' },
  { href: `${base}in2academia/`, label: 'In2Academia' },
  { href: `${base}events/`, label: 'Events' },
  { href: `${base}about/`, label: 'About' },
  { href: `${base}leadership/`, label: 'Leadership' },
  { href: `${base}contact/`, label: 'Contact' },
];

const here = Astro.url.pathname;
const isActive = (href: string) => here.startsWith(href);
---
<a class="skip" href="#main">Skip to content</a>
<header class="site-header">
  <div class="page-shell bar">
    <a class="wordmark" href={base}>Anthropy</a>
    <nav aria-label="Primary">
      <ul>
        {links.map((link) => (
          <li>
            <a href={link.href} aria-current={isActive(link.href) ? 'page' : undefined}>
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style>
  .skip {
    position: absolute;
    left: -9999px;
  }
  .skip:focus {
    left: var(--s-4);
    top: var(--s-4);
    z-index: var(--z-skip);
    background: var(--c-surface);
    padding: var(--s-2) var(--s-4);
  }
  .site-header {
    border-bottom: 1px solid var(--c-rule);
    background: var(--c-ground);
  }
  .bar {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--s-4);
    padding-block: var(--s-4);
  }
  .wordmark {
    font-family: var(--f-display);
    font-weight: 700;
    font-size: var(--t-lg);
    letter-spacing: -0.02em;
    text-decoration: none;
  }
  nav ul {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--s-4) var(--s-6);
    list-style: none;
    margin: 0;
    padding: 0;
  }
  nav a {
    font-size: var(--t-sm);
    color: var(--c-ink-soft);
    text-decoration: none;
  }
  nav a:hover { color: var(--c-ink); }
  nav a[aria-current='page'] {
    color: var(--c-ink);
    text-decoration: underline;
    text-decoration-color: var(--c-sage);
    text-underline-offset: 0.3em;
  }
</style>
